# OpenRouter Filter

一个纯前端的静态网站服务，用于便捷筛选最具性价比的模型。

## Requirements

1. 从上下文、输入/输出的价格/模态、支持的参数等方面筛选模型。
2. 一键复制 Model ID。
3. 屏蔽 OpenRouter Native 路由。
4. 对支持的参数进行本土化处理。
   如 `response_format` 显示为 **Response Format** 或者 **响应格式**。
5. 按照输出价格从小到大排列。

## 技术栈（始终使用稳定的最新版本）

1. 优先采用 shadcn/ui 提供的组件实现。
2. Design Token 是唯一的视觉设计规范。
3. 样式主要通过 Tailwind CSS 实现。
4. 组件本身不得形成彼此独立的视觉规范。

## 特殊需求

1. 输入/输出价格选项的最小值取最小非零值。
2. 增加复选框控制是否显示免费模型，默认是否。
   输入/输出其中一项免费即算作免费模型。
3. 上下文默认最小值 256K；可以手动选择更小的值。
4. 增加复选框控制是否显示 batch 模型，默认是否。
5. 增加复选框控制是否显示 Latest 模型，默认是否。